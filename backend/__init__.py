"""EduSense AI 360 backend package.

Hosts all domain logic: pipeline orchestration, camera capture, AI models,
analytics engines, remarks, session management, reporting, shared contracts,
and cross-feature services. The frontend depends on this package; this package
never imports the frontend (Clean Architecture dependency direction).
"""
